name: alerta_buenaventura
description: App de alerta ciudadana para Buenaventura
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.24.0'

dependencies:
  flutter:
    sdk: flutter
  google_maps_flutter: ^2.5.1
  geolocator: ^10.1.0
  geocoding: ^2.0.4
  firebase_core: latest_version
  cloud_firestore: latest_version
  firebase_auth: latest_version
  firebase_messaging: latest_version
  flutter_local_notifications: latest_version
  google_sign_in: latest_version
  image_picker: latest_version
  firebase_storage: latest_version
  geoflutterfire2: ^3.0.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/ 
